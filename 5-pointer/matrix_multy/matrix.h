#include <math.h>
#include <stdio.h>

int* matrix_multy(int* A, int aM, int aN, int* B, int bM, int bN){
    int sum;
    if (bM!=aN)
        return NULL;
    for (int i=0;i<aM;i++){
        sum=0;
        for (int j=0; j<bN;j++){
            
        }
    }
}
